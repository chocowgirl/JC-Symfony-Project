{% extends "base.html.twig" %}


{% block body %}
<style>
	body {
		font-family: Arial, sans-serif;
		background-color: #f0f0f0;
		display: flex;
		justify-content: center;
		align-items: center;
		height: 100vh;
	}

	.welcome-container {
		background-color: #f0f0f0;
		width: 400px;
		border-radius: 15px;
		padding: 20px;
		box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
		text-align: left;
	}

	 /* Welcome header styling */
    .welcome-header {
        background: linear-gradient(90deg, #ff5f57, #ff8a65);
        padding: 20px;
        border-radius: 15px 15px 15px 15px;
        color: grey;
        font-size: 0.9em;
        font-weight: bold;
        /* text-transform: uppercase; */
    }

	.welcome-header h1{
		color: white;
	}

	.action-container {
		background-color: #f0f0f0;
		border-radius: 10px;
	}

	.navItem {
		font-size: 0.7em;
	}

	.response {
		color:black;
	}

</style>


{# <div class="action-container">
	<div class="nav">
		<div class="navItem">
			<a href="{{ path('app_welcome') }}">🏠 Home Screen</a>
		</div>
		<div class="navItem" onclick="window.location.href='{{ path('app_run_manager') }}'">
			<a href="{{ path('app_run_manager') }}">🏃‍➡️ Access Run Manager</a>
		</div>
		<div class="navItem" onclick="window.location.href='{{ path('app_shoe_manager') }}'">
			<a href="{{ path('app_shoe_manager') }}">👟 Access Shoe Manager</a>
		</div>
	</div> #}
	<div class="welcome-container">
		<div class="welcome-header">
		{# <div class="action-container"> #}
			<div class="nav">
				<div class="navItem">
					<a href="{{ path('app_welcome') }}">🏠 Home Screen</a>
				</div>
				<div class="navItem" onclick="window.location.href='{{ path('app_run_manager') }}'">
					<a href="{{ path('app_run_manager') }}">🏃‍➡️ Run Manager</a>
				</div>
				<div class="navItem" onclick="window.location.href='{{ path('app_shoe_manager') }}'">
					<a href="{{ path('app_shoe_manager') }}">👟 Shoe Manager</a>
				</div>
			</div>
			<h1>🔍 Search Runs</h1>
				{{ form_start(form) }}
					{{ form_row(form.startDate) }}
					{{ form_row(form.endDate) }}
					{{ form_row(form.shoe) }}
					<button type="submit">Search</button>


			<div class="action-container">
					{% if totalRuns > 0 %}
						<h4>Results</h4>
						<p>Total Runs:<br><span class="response"> {{ totalRuns }}</span></p>
						<p>Total Distance:<br><span class="response"> {{ totalDistance }} km</span></p>
						<p>Average Distance/Run:<br><span class="response"> {{ averageDistance }} km</span></p>
						<p>Average Speed per Km:<br><span class="response"> {{ averageSpeedPerKm | number_format(2)}} minutes/km</span></p>

						{# <ul>
							{% for activity in activities %}
								<li>{{ activity.activityDate|date('Y-m-d') }} - {{ activity.activityDistanceKm }} km - {{ activity.activityChronoMin }} minutes</li>
							{% endfor %}
						</ul> #}
					{% else %}
						<p>No activities found for the selected period.</p>
					{% endif %}
				{{ form_end(form) }}
				{# {{ dump(test) }} #}
			</div>
		</div>

	</div>





{# </div> #}
	</div>
	{# <h1>Search Runs</h1> #}



{% endblock %}


{# Add new run

	<style>
        .homebtn {
			background-color:#a0a0a0;
			width : 30px;
			height : 30px;
			border-radius: 15px;
			text-align:center;
			border-color: dark-gray;
			border-style: solid;
			border-width: 1px;
		}
	</style>



{{ form_start (form) }}
{{ form_widget (form) }}





<input type="submit" class="btn" value="Envoyer" />
{{ form_end (form) }}

	<div class="homebtn">
		<a href="{{ path('app_welcome')}}">🏠</a>
	<div> #}

{# {% endblock %} #}